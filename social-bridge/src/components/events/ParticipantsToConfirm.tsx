type Props = {
  eventId: string;
};

export default function ParticipantsToConfirm({ eventId }: Props) {
  // there organization organizator can confirm or decline participants
  return <div>ParticipantsToConfirm</div>;
}
